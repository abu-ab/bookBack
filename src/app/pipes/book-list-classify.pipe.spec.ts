import { BookListClassifyPipe } from './book-list-classify.pipe';

describe('BookListClassifyPipe', () => {
  it('create an instance', () => {
    const pipe = new BookListClassifyPipe();
    expect(pipe).toBeTruthy();
  });
});
