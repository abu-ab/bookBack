import { BorrowStatePipe } from './borrow-state.pipe';

describe('BorrowStatePipe', () => {
  it('create an instance', () => {
    const pipe = new BorrowStatePipe();
    expect(pipe).toBeTruthy();
  });
});
